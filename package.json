{
  "name": "weather-prediction-backend",
  "version": "1.0.0",
  "description": "Weather prediction backend API",
  "scripts": {
    "start": "cd backend/api && gunicorn --bind 0.0.0.0:$PORT app:app"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react-slick": "^0.31.0",
    "slick-carousel": "^1.8.1"
  }
}
